import React from "react"

const TaskDone = ( props ) =>{
   return(
      <div>
         <h1>Welocme to the Task Done Page..</h1>
         <p>Its contain all the work done by teachers <br/> including new data feeding , updation of data, deletion of data </p>
      </div>
   );
}

export default TaskDone;